## Packages
framer-motion | For cinematic entry animations and smooth transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Authentication is handled via useAuth hook
Images use TMDB poster paths which need a base URL (https://image.tmdb.org/t/p/w500)
Dark mode is the default and only theme
